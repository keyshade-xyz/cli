pub fn add(_wrkspc: &String, _prjct: Option<&String>) {
    let mut _api_key_input: String = String::new();
    let mut _private_key_input: String = String::new();

    println!("Add a new project to the workspace or a new workspace")
}
